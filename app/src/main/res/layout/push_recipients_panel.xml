<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/recipients_panel"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <org.jimvixx.smsecure.contacts.RecipientsEditor
        android:id="@+id/recipients_text"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:inputType="text|textCapSentences|textAutoCorrect"
        android:hint="@string/recipients_panel__to"
        android:paddingEnd="45dip"
        android:paddingStart="0dip"
        android:singleLine="true"
        android:textColor="?attr/conversation_editor_text_color" />

    <ImageButton
        android:id="@+id/contacts_button"
        android:layout_width="40dip"
        android:layout_height="35dip"
        android:layout_alignEnd="@id/recipients_text"
        android:layout_marginTop="4dip"
        android:layout_marginEnd="5dip"
        android:background="@color/transparent"
        android:contentDescription="@null"
        android:maxWidth="32dip"
        android:maxHeight="32dip"
        android:src="@drawable/ic_plus_circle_outline" />

</RelativeLayout>